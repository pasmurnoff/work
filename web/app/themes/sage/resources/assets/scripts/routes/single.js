import singleSection from '../components/single-page/init';

export default {
    init() {
        singleSection()
    },
    finalize() {
        // JavaScript to be fired on the page, after the init JS
    },
};
